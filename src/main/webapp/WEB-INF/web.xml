<web-app xmlns="https://jakarta.ee/xml/ns/jakartaee"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="https://jakarta.ee/xml/ns/jakartaee
                             https://jakarta.ee/xml/ns/jakartaee/web-app_5_0.xsd"
         version="5.0">


	<!-- Chỉ định trang mặc định khi truy cập root của ứng dụng -->
	<welcome-file-list>
		<welcome-file>index.jsp</welcome-file>
	</welcome-file-list>

	<!-- Cấu hình session -->
	<session-config>
		<!-- Sử dụng COOKIE để lưu session id -->
		<tracking-mode>COOKIE</tracking-mode>
	</session-config>

	<!-- Filter 1: SiteMesh (dùng cho layout trang web) -->
	<filter>
		<filter-name>sitemesh</filter-name>
		<filter-class>org.sitemesh.config.ConfigurableSiteMeshFilter</filter-class>
	</filter>
	<filter-mapping>
		<!-- Áp dụng cho mọi URL trong ứng dụng -->
		<filter-name>sitemesh</filter-name>
		<url-pattern>/*</url-pattern>
	</filter-mapping>
	
	<!-- Filter 2: LogFilter (ghi log request) -->
	<filter>
		<filter-name>logFilter</filter-name>
		<filter-class>vn.iotstar.filter.LogFilter</filter-class>
	</filter>
	<filter-mapping>
		<filter-name>logFilter</filter-name>
		<url-pattern>/*</url-pattern>
	</filter-mapping>
</web-app>